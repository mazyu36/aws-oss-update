---
title: "AWS CDK v2.200.2 リリース解説"
version: "v2.200.2"
repository: "cdk"
repositoryDisplayName: "AWS CDK"
releaseType: "stable"
date: 2025-06-12
summary: "region-infoモジュールでのモジュール読み込みエラーを修正するパッチリリース"
releaseUrl: "https://github.com/aws/aws-cdk/releases/tag/v2.200.2"
---

## 概要

AWS CDK v2.200.2は、v2.200.1で発生した`@aws-cdk/region-info`モジュールの読み込みエラーを修正するパッチリリースです。このリリースでは、`aws-cdk-lib`から`errors.js`が正しくエクスポートされるようになり、`region-info`モジュールが正常に動作するようになります。

## バグ修正

### region-infoモジュールの読み込みエラー修正

**PR:** [#34691](https://github.com/aws/aws-cdk/pull/34691)
**Issue:** [#34690](https://github.com/aws/aws-cdk/issues/34690)

#### 問題の詳細

v2.200.1で導入されたPR #33384により、`@aws-cdk/region-info`モジュールが`aws-cdk-lib/core/lib/errors`から`UnscopedValidationError`をインポートするようになりましたが、`aws-cdk-lib`の`package.json`から`errors.js`が正しくエクスポートされていませんでした。

これにより、以下のようなモジュール読み込みエラーが発生していました:

```
Error: Cannot find module 'aws-cdk-lib/core/lib/errors'
Require stack:
- /node_modules/@aws-cdk/region-info/lib/fact.js
- /node_modules/@aws-cdk/region-info/lib/index.js
```

#### 修正内容

`packages/aws-cdk-lib/package.json`から`errors.js`を正しくエクスポートすることで、`@aws-cdk/region-info`モジュールが正常に動作するようになりました。

#### 影響を受けるコード

v2.200.1を使用していて、以下のようなコードでエラーが発生していた場合、v2.200.2にアップグレードすることで解決します:

```typescript
// v2.200.1ではこのインポートでエラーが発生していました
import { Fact } from '@aws-cdk/region-info';

// v2.200.2では正常に動作します
const endpoint = Fact.find('us-east-1', 'service:s3:endpoint');
```

## Alpha modules (2.200.2-alpha.0)

このリリースには、Alphaモジュールに対する変更は含まれていません。

## まとめ

AWS CDK v2.200.2は、v2.200.1で発生した`@aws-cdk/region-info`モジュールの読み込みエラーを修正する重要なパッチリリースです。v2.200.1を使用している場合は、速やかにv2.200.2にアップグレードすることを推奨します。

このリリースには破壊的変更や新機能は含まれておらず、単純なバグ修正のみが含まれているため、安全にアップグレードできます。
